---
phase: 03-ai-generation-and-manual-delivery
plan: 04
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - backend/app/api/newsletters.py
  - backend/app/schemas.py
  - backend/tests/test_newsletters.py
  - frontend/src/App.tsx
  - frontend/src/features/newsletters/NewsletterPreviewPage.tsx
autonomous: true
requirements:
  - GEN-03
must_haves:
  truths:
    - Operator can distinguish generation failures from delivery failures.
    - Generation/send results use one normalized response shape.
    - The newsletter workflow shows enough run feedback to confirm what happened.
  artifacts:
    - backend/app/api/newsletters.py
    - frontend/src/features/newsletters/NewsletterPreviewPage.tsx
  key_links:
    - backend result payloads map cleanly into UI feedback
    - generation and send actions do not expose raw provider-specific error shapes
---

<objective>
Normalize result and error handling for generation and manual delivery.

Purpose: The operator needs actionable feedback, not raw provider transport noise.
Output: Normalized response shapes and UI feedback for generation/send actions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-generation-and-manual-delivery/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize generation and send result payloads</name>
  <files>backend/app/api/newsletters.py, backend/app/schemas.py, backend/tests/test_newsletters.py, frontend/src/App.tsx, frontend/src/features/newsletters/NewsletterPreviewPage.tsx</files>
  <read_first>.planning/phases/03-ai-generation-and-manual-delivery/03-CONTEXT.md, .planning/research/PITFALLS.md</read_first>
  <action>Refine the API response shapes and UI handling so generation vs delivery errors are explicit and actionable. Cover the normalized shape in tests and surface it in the UI without leaking provider-specific implementation details.</action>
  <verify>backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py && npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - generation and send actions return normalized status/result payloads
    - UI messages distinguish generation failures from delivery failures
    - tests cover normalized result handling
  </acceptance_criteria>
  <done>Operator-facing result handling is normalized and clear.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py`
- [ ] `npm --prefix frontend run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Operator-facing result handling is clear enough for real use
  </success_criteria>

<output>
After completion, create `.planning/phases/03-ai-generation-and-manual-delivery/03-04-SUMMARY.md`
</output>
