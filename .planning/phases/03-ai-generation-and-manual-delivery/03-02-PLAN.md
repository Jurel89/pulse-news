---
phase: 03-ai-generation-and-manual-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - backend/app/models.py
  - backend/app/schemas.py
  - backend/app/api/newsletters.py
  - backend/tests/test_newsletters.py
autonomous: true
requirements:
  - GEN-04
must_haves:
  truths:
    - Every manual generation or delivery attempt creates an immutable run record first.
    - Run records snapshot subject, body, template, provider, model, and recipients.
    - Run records are stored separately from the mutable newsletter definition.
  artifacts:
    - backend/app/models.py
    - backend/app/api/newsletters.py
  key_links:
    - generation and send endpoints write run records before performing work
    - run snapshots preserve generated or sent content even if the newsletter changes later
---

<objective>
Introduce immutable run snapshots and run persistence.

Purpose: Manual delivery and later scheduling both need a run-first execution model to avoid duplicate-send and lost-history pitfalls.
Output: Run models, snapshot persistence, and backend behavior that creates runs before work executes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-generation-and-manual-delivery/03-CONTEXT.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run persistence and snapshot models</name>
  <files>backend/app/models.py, backend/app/schemas.py, backend/app/api/newsletters.py, backend/tests/test_newsletters.py</files>
  <read_first>.planning/research/PITFALLS.md, .planning/phases/03-ai-generation-and-manual-delivery/03-CONTEXT.md</read_first>
  <action>Add run models and snapshot fields, then wire generation/manual-send paths to create run records before execution. Extend tests to verify immutable run snapshots exist.</action>
  <verify>backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py</verify>
  <acceptance_criteria>
    - run tables exist and are populated before generation/send work begins
    - run snapshots keep subject/body/template/provider/model/recipient information
    - tests cover run creation
  </acceptance_criteria>
  <done>Run persistence exists and is used by execution paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Run snapshots exist before manual delivery lands
  </success_criteria>

<output>
After completion, create `.planning/phases/03-ai-generation-and-manual-delivery/03-02-SUMMARY.md`
</output>
