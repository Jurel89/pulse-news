---
phase: 03-ai-generation-and-manual-delivery
plan: 01
type: execute
wave: 1
depends_on:
  - "02-03"
files_modified:
  - backend/pyproject.toml
  - backend/app/config.py
  - backend/app/ai_generation.py
  - backend/app/api/newsletters.py
  - backend/tests/test_newsletters.py
  - frontend/src/lib/api.ts
  - frontend/src/App.tsx
autonomous: true
requirements:
  - GEN-01
  - GEN-02
  - GEN-03
must_haves:
  truths:
    - Operator can trigger draft generation using the configured provider/model settings.
    - Generation uses one provider abstraction service rather than provider-specific feature code.
    - Generation errors surface in a normalized result shape.
  artifacts:
    - backend/app/ai_generation.py
    - backend/app/api/newsletters.py
  key_links:
    - frontend generation action calls the backend provider service
    - provider service writes generated content back into newsletter draft fields
---

<objective>
Add the provider abstraction and draft-generation workflow.

Purpose: Draft generation is the first AI-specific capability and should land on top of the newsletter/editor structure that already exists.
Output: Backend generation service, generation endpoint, and UI action to populate draft fields.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-generation-and-manual-delivery/03-CONTEXT.md
@.planning/phases/02-draft-workflow-and-template-system/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the backend generation service and endpoint</name>
  <files>backend/pyproject.toml, backend/app/config.py, backend/app/ai_generation.py, backend/app/api/newsletters.py, backend/tests/test_newsletters.py</files>
  <read_first>.planning/research/STACK.md, .planning/phases/03-ai-generation-and-manual-delivery/03-CONTEXT.md</read_first>
  <action>Add a provider abstraction service using LiteLLM when configured and a local fallback when not. Expose a generation endpoint that writes generated subject/preheader/body into the newsletter draft fields and returns normalized success/error results.</action>
  <verify>backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py</verify>
  <acceptance_criteria>
    - backend generation endpoint exists and updates newsletter draft fields
    - provider abstraction is centralized in one service module
    - tests cover the local fallback or normalized generation result shape
  </acceptance_criteria>
  <done>Generation service exists and is test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Expose generation controls in the newsletter workflow</name>
  <files>frontend/src/lib/api.ts, frontend/src/App.tsx</files>
  <read_first>frontend/src/App.tsx, .planning/phases/03-ai-generation-and-manual-delivery/03-CONTEXT.md</read_first>
  <action>Add a generate-draft action in the newsletter workflow so operators can trigger generation and see generation errors/successes without leaving the editor flow.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - operator can trigger draft generation from the newsletter workflow
    - generated content updates the visible draft fields
    - errors are surfaced clearly
  </acceptance_criteria>
  <done>Generation controls are usable in the app shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py`
- [ ] `npm --prefix frontend run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- AI generation works through one backend abstraction layer
  </success_criteria>

<output>
After completion, create `.planning/phases/03-ai-generation-and-manual-delivery/03-01-SUMMARY.md`
</output>
