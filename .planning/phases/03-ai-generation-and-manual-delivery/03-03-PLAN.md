---
phase: 03-ai-generation-and-manual-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - backend/app/email_delivery.py
  - backend/app/api/newsletters.py
  - backend/tests/test_newsletters.py
  - frontend/src/lib/api.ts
  - frontend/src/features/newsletters/NewsletterPreviewPage.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - SEND-01
  - SEND-04
must_haves:
  truths:
    - Operator can manually send a newsletter to all active recipients from the UI.
    - Manual send uses the shared render and delivery boundaries.
    - Per-recipient outcomes are stored with the run.
  artifacts:
    - backend/app/api/newsletters.py
    - backend/app/email_delivery.py
  key_links:
    - preview/send UI calls the manual-send endpoint
    - manual-send endpoint records run and recipient outcomes
---

<objective>
Implement manual newsletter delivery.

Purpose: The product is not operational until the operator can intentionally send a newsletter to its audience.
Output: Manual-send endpoint, per-recipient send handling, and UI trigger flow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-generation-and-manual-delivery/03-CONTEXT.md
@.planning/phases/02-draft-workflow-and-template-system/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the delivery service for manual send runs</name>
  <files>backend/app/email_delivery.py, backend/app/api/newsletters.py, backend/tests/test_newsletters.py</files>
  <read_first>.planning/phases/03-ai-generation-and-manual-delivery/03-CONTEXT.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>Extend the delivery service to send to the newsletter’s active recipients and persist per-recipient send outcomes on the associated run. Cover the local fallback path in tests.</action>
  <verify>backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py</verify>
  <acceptance_criteria>
    - manual-send endpoint targets all active recipients
    - send outcomes are stored with the run
    - tests cover the manual-send result shape
  </acceptance_criteria>
  <done>Manual delivery is implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Add manual-send controls to the newsletter workflow</name>
  <files>frontend/src/lib/api.ts, frontend/src/features/newsletters/NewsletterPreviewPage.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/features/newsletters/NewsletterPreviewPage.tsx, .planning/phases/03-ai-generation-and-manual-delivery/03-CONTEXT.md</read_first>
  <action>Add a manual-send action to the preview workflow with clear result feedback so the operator can intentionally deliver a newsletter after previewing it.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - operator can trigger manual send from the UI
    - UI distinguishes test-send results from manual-send results
    - send result is visible immediately after action completion
  </acceptance_criteria>
  <done>Manual-send controls are usable in the UI.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py`
- [ ] `npm --prefix frontend run build`
- [ ] `docker build -t pulse-news:test .`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Manual delivery works through the same render and delivery boundaries already established
  </success_criteria>

<output>
After completion, create `.planning/phases/03-ai-generation-and-manual-delivery/03-03-SUMMARY.md`
</output>
