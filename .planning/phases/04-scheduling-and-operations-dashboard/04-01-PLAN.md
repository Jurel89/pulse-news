---
phase: 04-scheduling-and-operations-dashboard
plan: 01
type: execute
wave: 1
depends_on:
  - "03-03"
files_modified:
  - backend/pyproject.toml
  - backend/app/models.py
  - backend/app/scheduler.py
  - backend/app/main.py
  - backend/app/api/newsletters.py
  - backend/tests/test_newsletters.py
autonomous: true
requirements:
  - SEND-02
  - SEND-03
must_haves:
  truths:
    - Operator can define and toggle recurring schedules.
    - Scheduled jobs survive restart through persisted scheduling state or deterministic reconciliation.
    - Scheduled sends use the same execution path as manual sends.
  artifacts:
    - backend/app/scheduler.py
    - backend/app/api/newsletters.py
  key_links:
    - newsletter schedule changes reconcile scheduler jobs
    - scheduler jobs invoke the shared send execution path
---

<objective>
Add scheduler infrastructure and recurring send controls.

Purpose: The app is not operationally useful until recurring sends can be managed safely.
Output: APScheduler service, newsletter schedule controls, and shared scheduled execution path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-scheduling-and-operations-dashboard/04-CONTEXT.md
@.planning/phases/03-ai-generation-and-manual-delivery/03-03-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement APScheduler-backed scheduling and reconciliation</name>
  <files>backend/pyproject.toml, backend/app/models.py, backend/app/scheduler.py, backend/app/main.py, backend/app/api/newsletters.py, backend/tests/test_newsletters.py</files>
  <read_first>.planning/research/PITFALLS.md, .planning/phases/04-scheduling-and-operations-dashboard/04-CONTEXT.md</read_first>
  <action>Add APScheduler with persistent job storage or deterministic reconciliation, plus schedule enable/disable and startup sync logic. Scheduled jobs must route through the same send execution path as manual sends.</action>
  <verify>backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py</verify>
  <acceptance_criteria>
    - scheduler service exists and starts with the app
    - schedule changes reconcile jobs cleanly
    - tests cover schedule persistence or reconciliation behavior
  </acceptance_criteria>
  <done>Recurring scheduling backend is implemented and verified.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Scheduled and manual sends share one execution spine
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduling-and-operations-dashboard/04-01-SUMMARY.md`
</output>
