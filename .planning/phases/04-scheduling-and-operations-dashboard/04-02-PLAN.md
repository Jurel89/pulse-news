---
phase: 04-scheduling-and-operations-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - backend/app/schemas.py
  - backend/app/api/runs.py
  - backend/app/api/router.py
  - backend/tests/test_runs.py
  - frontend/src/features/dashboard/RunDashboardPage.tsx
  - frontend/src/lib/api.ts
  - frontend/src/App.tsx
  - frontend/src/styles.css
autonomous: true
requirements:
  - AUD-04
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - Operator can view a run dashboard with filters.
    - Operator can inspect a run detail with snapshots, recipients, and delivery outcomes.
    - Dashboard filters narrow history by newsletter, status, and date range.
  artifacts:
    - backend/app/api/runs.py
    - frontend/src/features/dashboard/RunDashboardPage.tsx
  key_links:
    - dashboard UI reads dedicated run APIs
    - run detail API exposes the stored run snapshot and outcomes
---

<objective>
Build the operations dashboard and run APIs.

Purpose: Operators need visibility into what happened before recurring sends can be trusted.
Output: Run list/detail backend endpoints and dashboard UI with filters.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-scheduling-and-operations-dashboard/04-CONTEXT.md
@.planning/phases/03-ai-generation-and-manual-delivery/03-VERIFICATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add run list/detail APIs and dashboard UI</name>
  <files>backend/app/schemas.py, backend/app/api/runs.py, backend/app/api/router.py, backend/tests/test_runs.py, frontend/src/features/dashboard/RunDashboardPage.tsx, frontend/src/lib/api.ts, frontend/src/App.tsx, frontend/src/styles.css</files>
  <read_first>.planning/phases/04-scheduling-and-operations-dashboard/04-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create dedicated run list/detail APIs with filters and build the dashboard screen that consumes them. The detail view must show stored snapshots and outcomes, not recomputed current newsletter state.</action>
  <verify>backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py backend/tests/test_runs.py && npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - run APIs support filters
    - dashboard UI exposes filters and details
    - detail view shows snapshot content and recipient outcomes
  </acceptance_criteria>
  <done>Dashboard visibility is implemented and verified.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py backend/tests/test_runs.py`
- [ ] `npm --prefix frontend run build`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Dashboard uses stored run history rather than mutable newsletter state
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduling-and-operations-dashboard/04-02-SUMMARY.md`
</output>
