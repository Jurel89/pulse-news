---
phase: 04-scheduling-and-operations-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - backend/app/api/newsletters.py
  - backend/app/api/runs.py
  - backend/tests/test_runs.py
  - frontend/src/features/dashboard/RunDashboardPage.tsx
autonomous: true
requirements:
  - SEND-03
must_haves:
  truths:
    - Scheduled execution records look like manual execution records except for trigger mode.
    - Dashboard clearly distinguishes manual vs scheduled runs.
  artifacts:
    - backend/app/api/newsletters.py
    - frontend/src/features/dashboard/RunDashboardPage.tsx
  key_links:
    - scheduled runs persist via the same run model
    - dashboard surfaces scheduled trigger mode
---

<objective>
Finalize the scheduled-run path and dashboard integration.

Purpose: Phase 4 should leave one coherent execution model, not separate manual and scheduled histories.
Output: Scheduled-run trigger mode handling and dashboard distinction between run types.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-scheduling-and-operations-dashboard/04-CONTEXT.md
@.planning/phases/04-scheduling-and-operations-dashboard/04-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Finish scheduled/manual run unification and dashboard distinction</name>
  <files>backend/app/api/newsletters.py, backend/app/api/runs.py, backend/tests/test_runs.py, frontend/src/features/dashboard/RunDashboardPage.tsx</files>
  <read_first>.planning/phases/04-scheduling-and-operations-dashboard/04-CONTEXT.md</read_first>
  <action>Ensure scheduled sends and manual sends share the same run model and that the dashboard clearly distinguishes them by trigger mode and status.</action>
  <verify>backend/.venv/bin/pytest backend/tests/test_runs.py && npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - trigger mode is visible and correct for scheduled runs
    - scheduled runs appear in the dashboard without special casing a second history path
  </acceptance_criteria>
  <done>Scheduled/manual execution is unified from an operator perspective.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/pytest backend/tests/test_runs.py`
- [ ] `npm --prefix frontend run build`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Scheduled and manual runs present coherently in the dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduling-and-operations-dashboard/04-03-SUMMARY.md`
</output>
