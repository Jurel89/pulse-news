---
phase: 05-compliance-and-delivery-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - backend/app/email_delivery.py
  - backend/app/models.py
  - backend/app/schemas.py
  - backend/app/api/runs.py
  - backend/tests/test_runs.py
  - frontend/src/features/dashboard/RunDashboardPage.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - DASH-04
must_haves:
  truths:
    - Operator can trigger delivery reconciliation for a run.
    - Reconciliation appends reviewable events to the run detail view.
    - Local and live modes are clearly distinguished in reconciliation results.
  artifacts:
    - backend/app/api/runs.py
    - frontend/src/features/dashboard/RunDashboardPage.tsx
  key_links:
    - run detail UI can request reconciliation and display new events
    - backend reconciliation reads stored provider IDs and records new events
---

<objective>
Add delivery reconciliation events and operator review.

Purpose: Delivery trust requires an explicit way to review post-send status updates.
Output: Run reconciliation events, reconciliation endpoint, and detail UI for those updates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-compliance-and-delivery-hardening/05-CONTEXT.md
@https://resend.com/docs/api-reference/emails/retrieve-email
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add reconciliation events and dashboard review UI</name>
  <files>backend/app/email_delivery.py, backend/app/models.py, backend/app/schemas.py, backend/app/api/runs.py, backend/tests/test_runs.py, frontend/src/features/dashboard/RunDashboardPage.tsx, frontend/src/lib/api.ts</files>
  <read_first>.planning/phases/05-compliance-and-delivery-hardening/05-CONTEXT.md</read_first>
  <action>Add run reconciliation event storage, a reconciliation endpoint that updates event history using live Resend status when possible and explicit fallback events otherwise, and dashboard detail UI to display those updates.</action>
  <verify>backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py backend/tests/test_runs.py && npm --prefix frontend run build && docker build -t pulse-news:test .</verify>
  <acceptance_criteria>
    - reconciliation endpoint exists and appends events
    - run detail UI shows reconciliation events
    - local vs live reconciliation results are explicit
  </acceptance_criteria>
  <done>Delivery reconciliation is implemented and reviewable.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/pytest backend/tests/test_auth.py backend/tests/test_newsletters.py backend/tests/test_runs.py`
- [ ] `npm --prefix frontend run build`
- [ ] `docker build -t pulse-news:test .`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Reconciliation updates are visible and trustworthy
</success_criteria>

<output>
After completion, create `.planning/phases/05-compliance-and-delivery-hardening/05-02-SUMMARY.md`
</output>
