---
phase: 01-foundation-and-secure-control-plane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/__init__.py
  - backend/app/config.py
  - backend/app/database.py
  - backend/app/main.py
  - backend/app/models.py
  - backend/app/schemas.py
  - backend/app/deps.py
  - backend/app/api/__init__.py
  - backend/app/api/router.py
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/styles.css
  - Dockerfile
  - .dockerignore
autonomous: true
requirements:
  - AUTH-03
  - NEWS-01
must_haves:
  truths:
    - Backend API starts successfully and exposes a health endpoint.
    - Frontend app builds successfully and can be served by the backend container in production mode.
    - SQLite-backed app data persists in a configured data directory.
  artifacts:
    - backend/app/main.py
    - backend/app/database.py
    - frontend/src/App.tsx
    - Dockerfile
  key_links:
    - frontend build output is served by the backend application
    - backend startup initializes the application database
---

<objective>
Create the project shell for backend, frontend, and container packaging.

Purpose: Establish a stable application structure so auth and newsletter CRUD can be implemented on top of a running full-stack baseline.
Output: Backend app skeleton, frontend app skeleton, container build, and database bootstrap.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-secure-control-plane/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the backend application skeleton</name>
  <files>backend/pyproject.toml, backend/app/__init__.py, backend/app/config.py, backend/app/database.py, backend/app/main.py, backend/app/models.py, backend/app/schemas.py, backend/app/deps.py, backend/app/api/__init__.py, backend/app/api/router.py</files>
  <read_first>.planning/PROJECT.md, .planning/research/STACK.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>Create the FastAPI project structure with typed config, SQLite database setup, shared dependencies, root health endpoint, API router registration, and production static-file serving hook points. Keep the structure ready for auth and newsletter routes that later plans will add.</action>
  <verify>python3 -m compileall backend/app</verify>
  <acceptance_criteria>
    - backend/app/main.py creates a FastAPI app and mounts the API router
    - backend/app/database.py configures a SQLite SQLAlchemy engine and session factory
    - backend/pyproject.toml declares the runtime dependencies required for the backend shell
  </acceptance_criteria>
  <done>Backend skeleton exists and compiles without syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create the frontend application shell</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/vite.config.ts, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/styles.css</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-foundation-and-secure-control-plane/01-CONTEXT.md</read_first>
  <action>Create a React + TypeScript + Vite operator UI shell with responsive base styling, routing-ready app structure, and a placeholder app frame that can host login, dashboard, and newsletter views in later plans.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/package.json includes build scripts
    - frontend/src/App.tsx renders a substantive responsive shell, not a placeholder sentence
    - frontend build output can be generated without TypeScript errors
  </acceptance_criteria>
  <done>Frontend shell builds successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Package the app into one container</name>
  <files>Dockerfile, .dockerignore, backend/app/main.py</files>
  <read_first>.planning/PROJECT.md, .planning/research/SUMMARY.md</read_first>
  <action>Create a single Dockerfile that builds the frontend and runs the FastAPI backend while serving the built frontend assets. Keep the image aligned with the one-container constraint and provide sane exclusions in .dockerignore.</action>
  <verify>docker build -t pulse-news:test .</verify>
  <acceptance_criteria>
    - Dockerfile builds both frontend and backend into one runnable image
    - backend/app/main.py serves the frontend build directory when it exists
    - .dockerignore excludes local caches and planning noise that should not enter the image
  </acceptance_criteria>
  <done>The application can be built as a single image.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m compileall backend/app`
- [ ] `npm --prefix frontend run build`
- [ ] `docker build -t pulse-news:test .`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Full-stack shell is ready for auth and CRUD work
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-secure-control-plane/01-01-SUMMARY.md`
</output>
