---
phase: 01-foundation-and-secure-control-plane
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/app/auth.py
  - backend/app/security.py
  - backend/app/api/auth.py
  - backend/app/api/router.py
  - backend/app/models.py
  - backend/app/schemas.py
  - frontend/src/lib/api.ts
  - frontend/src/lib/session.ts
  - frontend/src/features/auth/LoginPage.tsx
  - frontend/src/features/settings/AccountPage.tsx
  - frontend/src/App.tsx
  - frontend/src/styles.css
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - First-run bootstrap allows creation of the initial operator account once and only once.
    - Operator can log in and log out with a protected session cookie.
    - Authenticated session protects admin application routes and API endpoints.
    - Operator can change their password from the application.
  artifacts:
    - backend/app/api/auth.py
    - backend/app/security.py
    - frontend/src/features/auth/LoginPage.tsx
    - frontend/src/features/settings/AccountPage.tsx
  key_links:
    - login UI calls backend auth endpoints and establishes a session
    - protected frontend shell reacts to authenticated session state
    - password change flow updates persisted credentials securely
---

<objective>
Implement secure single-user authentication and session handling.

Purpose: Phase 1 is not complete unless the operator UI is properly protected and account management works without direct database intervention.
Output: Bootstrap, login, logout, session-check, and password-change flows across backend and frontend.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-secure-control-plane/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement backend auth and secure credential handling</name>
  <files>backend/app/auth.py, backend/app/security.py, backend/app/api/auth.py, backend/app/api/router.py, backend/app/models.py, backend/app/schemas.py</files>
  <read_first>.planning/research/PITFALLS.md, .planning/REQUIREMENTS.md</read_first>
  <action>Implement single-user auth APIs for bootstrap, login, logout, session introspection, and password change. Use secure password hashing from the Python standard library, signed session cookies, and explicit bootstrap locking after the first account is created.</action>
  <verify>pytest backend/tests/test_auth.py</verify>
  <acceptance_criteria>
    - bootstrap endpoint refuses account creation after the first operator exists
    - login sets a usable authenticated session cookie
    - password change requires the current password and updates stored credentials securely
  </acceptance_criteria>
  <done>Backend auth flows are secure and test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Implement frontend auth flows and protected shell behavior</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/session.ts, frontend/src/features/auth/LoginPage.tsx, frontend/src/features/settings/AccountPage.tsx, frontend/src/App.tsx, frontend/src/styles.css</files>
  <read_first>.planning/phases/01-foundation-and-secure-control-plane/01-CONTEXT.md, frontend/src/App.tsx</read_first>
  <action>Build the login/bootstrap experience, session-aware app shell, logout control, and account settings/password-change UI. Keep the UI responsive and operational rather than decorative.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - unauthenticated users only see bootstrap/login screens
    - authenticated users see the protected shell and can log out
    - account settings page allows password changes
  </acceptance_criteria>
  <done>Frontend auth flows work against the backend auth APIs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest backend/tests/test_auth.py`
- [ ] `npm --prefix frontend run build`
- [ ] protected-route behavior verified via API tests or integration checks
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Secure single-user auth is in place for the admin app
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-secure-control-plane/01-02-SUMMARY.md`
</output>
