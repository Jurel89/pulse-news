---
phase: 01-foundation-and-secure-control-plane
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - backend/app/api/newsletters.py
  - backend/app/api/router.py
  - backend/app/models.py
  - backend/app/schemas.py
  - frontend/src/features/newsletters/NewsletterListPage.tsx
  - frontend/src/features/newsletters/NewsletterEditorPage.tsx
  - frontend/src/features/newsletters/newsletter-types.ts
  - frontend/src/App.tsx
  - frontend/src/lib/api.ts
  - backend/tests/test_newsletters.py
autonomous: true
requirements:
  - NEWS-01
  - NEWS-02
  - NEWS-03
  - NEWS-04
must_haves:
  truths:
    - Operator can create and edit newsletter definitions from the UI.
    - Newsletter definitions persist with name, prompt, provider/model, template, and schedule settings.
    - Operator can archive/pause newsletters without deleting historical continuity.
    - Operator can delete a newsletter intentionally and the deletion is auditable.
  artifacts:
    - backend/app/api/newsletters.py
    - frontend/src/features/newsletters/NewsletterListPage.tsx
    - frontend/src/features/newsletters/NewsletterEditorPage.tsx
    - backend/tests/test_newsletters.py
  key_links:
    - newsletter list UI loads data from the backend newsletter API
    - newsletter editor form creates and updates persisted newsletter definitions
    - archive/delete actions map cleanly to backend state transitions
---

<objective>
Implement the newsletter CRUD core across backend and frontend.

Purpose: The product needs a real managed object before later phases can attach recipients, templates, generation, sending, or schedules.
Output: Newsletter APIs, persistence model, and responsive list/editor UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-secure-control-plane/01-CONTEXT.md
@.planning/phases/01-foundation-and-secure-control-plane/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement newsletter persistence and backend CRUD</name>
  <files>backend/app/api/newsletters.py, backend/app/api/router.py, backend/app/models.py, backend/app/schemas.py, backend/tests/test_newsletters.py</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>Add the newsletter model and CRUD API with create, list, retrieve, update, archive/pause, and delete actions. Persist the fields later phases need: prompt, provider/model config, template key, schedule expression, and notes. Record delete actions in an auditable way instead of silently dropping state.</action>
  <verify>pytest backend/tests/test_newsletters.py</verify>
  <acceptance_criteria>
    - newsletter CRUD endpoints are authenticated
    - newsletter records include the core configuration fields needed by later phases
    - archive and delete actions are behaviorally distinct
  </acceptance_criteria>
  <done>Newsletter backend CRUD is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Build newsletter list and editor UI</name>
  <files>frontend/src/features/newsletters/NewsletterListPage.tsx, frontend/src/features/newsletters/NewsletterEditorPage.tsx, frontend/src/features/newsletters/newsletter-types.ts, frontend/src/App.tsx, frontend/src/lib/api.ts</files>
  <read_first>frontend/src/App.tsx, .planning/phases/01-foundation-and-secure-control-plane/01-CONTEXT.md</read_first>
  <action>Create the responsive newsletter management UI with list, create, edit, archive, and delete flows. Fit it into the protected shell established in Plan 02.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - operator can create and edit newsletters from the UI
    - list view shows meaningful newsletter status and configuration summary
    - archive/delete actions are visible and intentional
  </acceptance_criteria>
  <done>Newsletter CRUD UI works against the backend APIs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest backend/tests/test_newsletters.py`
- [ ] `npm --prefix frontend run build`
- [ ] newsletter CRUD flows verified via API tests and manual smoke checks
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Newsletter CRUD foundation is ready for later recipient/template/generation phases
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-secure-control-plane/01-03-SUMMARY.md`
</output>
