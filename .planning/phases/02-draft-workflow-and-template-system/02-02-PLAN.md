---
phase: 02-draft-workflow-and-template-system
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/app/email_templates.py
  - backend/app/api/newsletters.py
  - backend/app/schemas.py
  - backend/tests/test_newsletters.py
  - frontend/src/features/newsletters/NewsletterPreviewPage.tsx
  - frontend/src/features/newsletters/newsletter-types.ts
  - frontend/src/lib/api.ts
  - frontend/src/App.tsx
  - frontend/src/styles.css
autonomous: true
requirements:
  - TPL-01
  - TPL-02
must_haves:
  truths:
    - Operator can choose between distinct template families.
    - Operator can preview rendered HTML and plain-text outputs for the current newsletter draft.
    - Preview output is derived from the same backend render layer that later send flows will use.
  artifacts:
    - backend/app/email_templates.py
    - frontend/src/features/newsletters/NewsletterPreviewPage.tsx
  key_links:
    - newsletter editor passes template and draft data to the preview API
    - backend preview API returns HTML and text rendered by the template system
---

<objective>
Implement the themed template renderer and draft preview flow.

Purpose: The operator needs confidence in how a newsletter will look and read before any send action is attempted.
Output: Backend template families, preview endpoint, and HTML/plain-text preview UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-draft-workflow-and-template-system/02-CONTEXT.md
@.planning/phases/02-draft-workflow-and-template-system/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the backend template renderer and preview API</name>
  <files>backend/app/email_templates.py, backend/app/api/newsletters.py, backend/app/schemas.py, backend/tests/test_newsletters.py</files>
  <read_first>.planning/research/STACK.md, .planning/phases/02-draft-workflow-and-template-system/02-CONTEXT.md</read_first>
  <action>Create a small curated template renderer with visually distinct families and a preview endpoint that returns HTML and plain-text output for a newsletter draft. Add tests that verify preview generation across template choices.</action>
  <verify>backend/.venv/bin/pytest backend/tests/test_newsletters.py</verify>
  <acceptance_criteria>
    - backend exposes preview output in HTML and plain-text forms
    - at least two distinct template families render differently
    - preview tests validate backend rendering behavior
  </acceptance_criteria>
  <done>Backend preview rendering is available and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Add preview UX to the newsletter workflow</name>
  <files>frontend/src/features/newsletters/NewsletterPreviewPage.tsx, frontend/src/features/newsletters/newsletter-types.ts, frontend/src/lib/api.ts, frontend/src/App.tsx, frontend/src/styles.css</files>
  <read_first>frontend/src/App.tsx, .planning/phases/02-draft-workflow-and-template-system/02-CONTEXT.md</read_first>
  <action>Expose template choice and preview actions in the UI, including HTML preview and plain-text preview surfaces. Keep the preview flow adjacent to the existing newsletter editor rather than creating a separate mode-switching maze.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - operator can select a template family in the editor
    - operator can open an HTML preview and plain-text preview
    - preview UI is responsive and integrated with the existing shell
  </acceptance_criteria>
  <done>Preview UX is available in the app shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/pytest backend/tests/test_newsletters.py`
- [ ] `npm --prefix frontend run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Preview flows use the real template renderer
  </success_criteria>

<output>
After completion, create `.planning/phases/02-draft-workflow-and-template-system/02-02-SUMMARY.md`
</output>
