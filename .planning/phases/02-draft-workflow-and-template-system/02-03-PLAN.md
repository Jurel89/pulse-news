---
phase: 02-draft-workflow-and-template-system
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - backend/app/email_delivery.py
  - backend/app/config.py
  - backend/app/api/newsletters.py
  - backend/tests/test_newsletters.py
  - frontend/src/features/newsletters/NewsletterPreviewPage.tsx
  - frontend/src/lib/api.ts
  - frontend/src/App.tsx
  - frontend/src/styles.css
autonomous: true
requirements:
  - TPL-03
must_haves:
  truths:
    - Operator can trigger a test send to a specified address from the UI.
    - Test send uses the same render output that the preview shows.
    - Local development without Resend credentials still records a meaningful test-send result instead of hard failing silently.
  artifacts:
    - backend/app/email_delivery.py
    - backend/app/api/newsletters.py
    - frontend/src/features/newsletters/NewsletterPreviewPage.tsx
  key_links:
    - preview/test-send UI calls the backend test-send endpoint
    - backend test-send endpoint uses the shared render service and delivery boundary
---

<objective>
Implement test-send capability on top of the preview and render foundation.

Purpose: The operator needs a confidence loop before live delivery and AI automation arrive.
Output: Test-send endpoint, Resend-capable delivery boundary, local fallback behavior, and UI trigger flow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-draft-workflow-and-template-system/02-CONTEXT.md
@.planning/phases/02-draft-workflow-and-template-system/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a backend test-send delivery service</name>
  <files>backend/app/email_delivery.py, backend/app/config.py, backend/app/api/newsletters.py, backend/tests/test_newsletters.py</files>
  <read_first>.planning/research/SUMMARY.md, .planning/phases/02-draft-workflow-and-template-system/02-CONTEXT.md</read_first>
  <action>Create a delivery service that can send a test email through Resend when configured and otherwise returns a local development fallback result with rendered payload details. Add backend coverage for the test-send route and result shape.</action>
  <verify>backend/.venv/bin/pytest backend/tests/test_newsletters.py</verify>
  <acceptance_criteria>
    - backend exposes a test-send endpoint for newsletters
    - delivery service supports a configured Resend path plus local fallback behavior
    - tests verify test-send response behavior
  </acceptance_criteria>
  <done>Backend test-send flow exists and is covered.</done>
</task>

<task type="auto">
  <name>Task 2: Expose test-send controls in the UI</name>
  <files>frontend/src/features/newsletters/NewsletterPreviewPage.tsx, frontend/src/lib/api.ts, frontend/src/App.tsx, frontend/src/styles.css</files>
  <read_first>frontend/src/features/newsletters/NewsletterPreviewPage.tsx, .planning/phases/02-draft-workflow-and-template-system/02-CONTEXT.md</read_first>
  <action>Add a test-send form to the preview workflow so the operator can target one address, trigger a send, and see the result clearly in the UI.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - operator can enter a test address and trigger a test send
    - UI surfaces success/fallback/failure results clearly
    - test-send stays adjacent to preview rather than becoming a detached workflow
  </acceptance_criteria>
  <done>Test-send controls work in the app shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/pytest backend/tests/test_newsletters.py`
- [ ] `npm --prefix frontend run build`
- [ ] `docker build -t pulse-news:test .`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Test send is available without undermining the later production delivery phase
  </success_criteria>

<output>
After completion, create `.planning/phases/02-draft-workflow-and-template-system/02-03-SUMMARY.md`
</output>
